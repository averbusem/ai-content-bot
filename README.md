# 🤖 AI Content Bot

Telegram-бот для автоматизации создания контента для некоммерческих организаций (НКО). Помогает генерировать тексты постов, редактировать контент, создавать контент-планы и генерировать изображения с использованием AI-технологий.

## 📋 Описание

AI Content Bot — это интеллектуальный помощник для НКО, который использует возможности GigaChat и Salute Speech для создания качественного контента. Бот предоставляет удобный интерфейс для:

- ✍️ **Генерация текстов** — создание постов различными способами
- ✏️ **Редактирование текстов** — исправление ошибок и улучшение текста
- 📅 **Контент-планирование** — создание планов публикаций на заданный период
- 🎨 **Генерация изображений** — создание визуального контента
- 📊 **Работа с данными НКО** — управление информацией об организации

## 🏗️ Структура проекта

```
ai-content-bot/
├── src/                          # Исходный код приложения
│   ├── main.py                   # Точка входа приложения
│   ├── config.py                 # Конфигурация и настройки
│   │
│   ├── bot/                      # Модуль Telegram-бота
│   │   ├── __init__.py           # Инициализация бота и диспетчера
│   │   ├── keyboards.py          # Клавиатуры для интерфейса
│   │   ├── middlewares.py        # Middleware для обработки сообщений
│   │   ├── states.py             # FSM состояния бота
│   │   │
│   │   └── handlers/             # Обработчики команд и callback'ов
│   │       ├── start.py          # Обработчик команды /start
│   │       ├── menu.py           # Главное меню и навигация
│   │       ├── help.py           # Справка и помощь
│   │       ├── text_generation.py        # Генерация текстов
│   │       ├── text_generation_struct.py # Структурированная генерация
│   │       ├── text_editor.py            # Редактор текстов
│   │       ├── content_plan.py           # Создание контент-планов
│   │       ├── image_generation.py       # Генерация изображений
│   │       └── nko_data.py               # Работа с данными НКО
│   │
│   └── services/                 # Бизнес-логика и сервисы
│       ├── ai_manager.py         # Менеджер AI-сервисов
│       ├── content_generator.py  # Генератор контента
│       ├── image_generator.py    # Генератор изображений
│       ├── nko_service.py        # Сервис работы с НКО
│       │
│       └── models/               # Модели для работы с AI
│           ├── gigachat.py      # Интеграция с GigaChat
│           └── salute.py        # Интеграция с Salute Speech
│
├── docker-compose.yml            # Конфигурация Docker Compose
├── Dockerfile                    # Образ для контейнера бота
├── requirements.txt              # Python-зависимости
└── README.md                     # Документация проекта
```

## 🚀 Запуск проекта

### Предварительные требования

- Docker и Docker Compose
- Файл `.env` с необходимыми переменными окружения

### Переменные окружения

Создайте файл `.env` в корне проекта  на основе файла `.env.example`:

### Запуск

Для запуска проекта выполните команду:

```bash
docker compose up --build -d
```

Эта команда:
- Соберёт образы из Dockerfile (`--build`)
- Запустит контейнеры в фоновом режиме (`-d`)
- Запустит Redis и бота

### Остановка

Для остановки контейнеров:

```bash
docker compose down
```

Для остановки с удалением volumes:

```bash
docker compose down -v
```

## 📦 Технологии

- **Python 3.12** — язык программирования
- **aiogram 3.22.0** — асинхронный фреймворк для Telegram Bot API
- **Redis** — хранилище FSM-состояний и данных об НКО
- **GigaChat** — AI-модель для генерации текстов
- **Salute Speech** — AI-сервис для работы с контентом
- **Docker** — контейнеризация приложения
